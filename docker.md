# Enter running docker containter
```bash
docker exec -it <containter ID> bash
```

# Enter running docker containter as root
```bash
docker exec -it --workdir /path/to/enter --user root <containter ID> bash
```
